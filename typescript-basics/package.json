{
  "name": "typescript-basics",
  "version": "1.0.0",
  "description": "1. Compile the files using `tsc filename.ts`. It generates an .js file\r 2. Use `node filename.js` to run the js file",
  "main": "index.js",
  "scripts": {
    "start": "tsc index.ts && node out/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.10"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.116"
  }
}
